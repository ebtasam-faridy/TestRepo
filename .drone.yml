---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64
  
steps:
- name: another
  image: docker
  commands:
  - echo "Hellos"
  - sleep 2
  - echo "Done scm-go version update validation test"
  - uname -a
  - ls -la
